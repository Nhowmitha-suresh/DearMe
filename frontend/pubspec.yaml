name: dearme_frontend
description: "DearMe Flutter frontend (minimal demo for pale-pink animations & floral blink)"
publish_to: 'none'

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  lottie: ^2.2.0
  simple_animations: ^3.1.1
  flutter_svg: ^2.0.7
  percent_indicator: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
